const Admin = require('./admin');
const Service = require('./service');
const ServiceInstance = require('./serviceInstance');
const Key = require('./key');

module.exports = { Admin, Service, ServiceInstance, Key };
